import java.util.Scanner;

public class DayOneAssignment {
    public static void main(String[] args) {
        /*
             // 1. To add two given numbers and print the sum.
             int num1 = 10;
             int num2 = 20;

             int sum = num1 + num2;

             System.out.println("Sum of given two numbers is " + sum);
         */

        /* 
            // 2. To find the average of the two given numbers and print.
        
             float num1 = 10;
             float num2 = 20;

             float avg = (num1 + num2) / 2;

             System.out.println("Average of given two numbers is " + avg);

         */

        /*
             // 3. To decide whether a given number is even or odd.

             Scanner inp = new Scanner(System.in);

             System.out.println("Enter Number: ");
             int num = inp.nextInt();

             System.out.println("Entered number is " + num);

             if (num % 2 == 0) {
            
             System.out.println("Number is EVEN!");
            
             } else {
            
             System.out.println("Number is ODD!");
            
             }

             inp.close();
         */

        /*
             // 4. To find the factorial of a given number.
            
             Scanner inp = new Scanner(System.in);
            
             System.out.println("Enter Number: ");
             int num = inp.nextInt();
            
             System.out.println("Entered number is " + num);
            
             int fact = 1;
             for (int i = 1; i <= num; i++) {
            
             fact = fact * i;
             }
             System.out.println("Factorial of " + num + " is " + fact);

             inp.close();

         */

        /*
             // 5. Write a series of RAPTOR statements that determines if X has the value 1, 2, or 3.
            
             and prints out “ONE”, “TWO”, or “THREE” accordingly.
            
             Scanner inp = new Scanner(System.in);
            
             System.out.println("Enter Number from (1,2,3): ");
             int x = inp.nextInt();
            
             if (x == 1) {
            
             System.out.println("ONE");
             } else if (x == 2) {
            
             System.out.println("TWO");
             } else {
             System.out.println("THREE");
             }

             inp.close();
         */

        /*

            // 6. Create a flowchart which asks the user for the area of a circle,
                 computes the area, then displays the area.
            
            Scanner inp = new Scanner(System.in);
            
            System.out.println("Enter Radius of circle: ");
            int rad = inp.nextInt();
            
            double areaOfcircle;
            double pi = 3.142;
            
            areaOfcircle = pi * rad * rad;
            
            System.out.println("Area of circle is " + areaOfcircle);
            
            inp.close();
          
        */

        /*
            // 7. Create a flowchart which uses a loop to display the following:
            // 0
            // 1
            // 2
            // 3
            // 4
            // 5
            
            for (int i = 0; i <= 5; i++) {
            
            System.out.println(i);
            
            }
         */

        /*
            // 8. Create a flowchart which computes the sum, average, and product of two
            numbers then prints their values.
            
            Scanner inp = new Scanner(System.in);
            
            System.out.println("Enter Number 1: ");
            int num1 = inp.nextInt();
            
            System.out.println("Enter Number 1: ");
            int num2 = inp.nextInt();
            
            int sum = num1 + num2;
            int avg = (num1 + num2) / 2;
            int product = num1 * num2;
            
            
            System.out.println("Entered numbers are " + num1 + " and " + num2);
            System.out.println("Sum of the Numbers is " + sum);
            System.out.println("Average of the Numbers is " + avg);
            System.out.println("Product of the Numbers is " + product);
            
            inp.close();
         */
    }
}